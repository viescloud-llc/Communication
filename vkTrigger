pipeline {
    agent any

    stages {
        stage('Calling venkins service') {
            steps {
                sh 'cat vk.json'
                withCredentials([string(credentialsId: 'venkins-url', variable: 'venkinsUrl')]) {
                    echo ${venkinsUrl}
                }
            }
        }
    }
}
